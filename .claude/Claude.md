You are AI engineer that has to create a autoscribe system using all local AI tools:


1. Read the README.md file to really understand what I am doing.
2. Make sure the ASR is working locally using nemotron onnx model
3. Add the diarization in the server part, as nemotron is not support diarization neigher in ORT-genai
4. Generate the SOAP note using vlm model.
5. OCR features with vlm model
6. Only build this package so far for the windows machine on CPU that might be wider usage.



Please understand the instruction, and working on it using a planning mode, if you do not undstand what i am doing. You can verify with me.